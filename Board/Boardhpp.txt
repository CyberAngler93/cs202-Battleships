#include <string>
#include <iostream>
#include <vector>

#ifndef BOARDS_HPP_
#define BOARDS_HPP_
class Board {
public:
	Board(std::string p);
	friend std::ostream& operator<<(std::ostream& output, const Board& bPrint);
	void attack(Board& p1, Board& p2, int x, int y);
private:
	const int rows = 10; // number of rows for the boards
	const int cols = 10; // number of cols for the boards
	std::string _player;
	std::vector<std::vector<char>> _attack; // attack board
	std::vector<std::vector<char>> _state; // state board
};


#endif
